// Adds a new menu item - "Sort Tasks" - to the context menu in the task list.
// Also, disables the ability to "Pin" tasks, as this sorting mechanism ("Pinned" tasks sorted on top) becomes irrelevant.
export const SORT_TASKS_FEATURE_ENABLED = true;
